export class Settings {
    constructor({defaultFPS, defaultThreshold, defaultTimeout}) {
        this.fps = defaultFPS;
        this.threshold = defaultThreshold;
        this.timeout = defaultTimeout
    }
}